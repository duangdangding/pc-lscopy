## lscopy v1.2.1

### 🎛️ 互传文件体验优化

- **发送成功提示自动关闭**：全部发送成功后的结果提示改为底部 toast，30 秒倒计时自动关闭（可点 × 提前关闭），不再挡住操作；有失败时仍保留手动确认，避免错过错误信息
- **传输记录只保留接收成功的文件**：发送记录与接收失败/拒绝/超时的条目不再写入历史，列表更干净（旧版本残留的历史记录也不再显示）
- **记录可单条删除**：每条记录新增「删除」按钮；单条删除和「清空」时都会询问——取消 / 仅删除记录 / 连同文件一起删除（只删接收保存的文件，不会误删发送源文件）

### 说明

- 无配置与协议变更，1.2.0 直接覆盖安装即可
