## lscopy v1.2.0

### ✨ 新功能：局域网互传文件

全新独立窗口（托盘菜单「互传文件」，或 设置 → 局域网同步 → 设备卡「互传文件」按钮进入），与剪贴板同步互不影响：

- **免配对直发**：实时扫描在线设备（3 秒轮询 + 10 秒深度扫描），点击选中即可发送；也支持**手动输入 IP** 直接发送
- **接收确认**：默认每次收到文件都弹窗确认（设备名 / IP / 文件名 / 大小，60 秒无操作自动拒绝）；开启「**自动接收**」后无需逐次确认
- **拖拽发送**：把文件拖进窗口即可发出，支持多文件
- **传输记录**：收发双方均记录（保留最近 200 条），失败原因可见，成功记录可一键「打开位置」
- **存储路径**：收到的文件保存到局域网同步的「文件存储路径」（未设置时用系统下载目录），重名自动加序号
- **加密传输**：已配对设备之间自动加密；大文件全程流式传输，不占内存
- 注意：对方是旧版 lscopy 或安卓端时无法接收，会提示「对方应用版本过旧」

### ⚠️ 升级提醒

- **应用标识（identifier）已变更为 `com.lsh.lscopy`**：新版本会被系统视为独立应用。建议先卸载旧版再安装 1.2.0，避免开机自启出现两条、单实例检测失效。
- 配置文件、数据库与局域网配对数据不受影响，升级后原样保留。

### 其他

- 版本号统一升至 1.2.0（`package.json` / `Cargo.toml` / `tauri.conf.json`）

---

**完整变更**：新增 `transfer.html` / `src/transfer.ts` / `src-tauri/src/lan/transfer.rs`，同步服务新增 `POST /recv` 端点（流式接收、黑名单校验、手动/自动确认通道）。
